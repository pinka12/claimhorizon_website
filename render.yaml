services:
  - type: web
    name: claimhorizon-frontend
    env: static
    buildCommand: corepack enable && corepack prepare pnpm@9 --activate && pnpm install && pnpm build
    staticPublishPath: artifacts/claimhorizon/dist